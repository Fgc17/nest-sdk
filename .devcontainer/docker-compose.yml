version: "3.8"

services:
  app:
    container_name: nest-sdk
    build:
      context: .
      dockerfile: Dockerfile

    volumes:
      - ../..:/workspaces:cached

    command: sleep infinity

    ports:
      - 3000:3000

    extra_hosts:
      - "host.docker.internal:host-gateway"